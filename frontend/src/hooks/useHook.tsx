/**
 * hooks
 *
 * This folder is meant for reusable hooks
 * in casdes were the hook is used only in one component or page
 * then that hook file should be in the correct `pages` sub-folder
 *
 * |- src
 * |-- pages
 * |--- subfolder
 * |---- useHookName.tsx
 */
