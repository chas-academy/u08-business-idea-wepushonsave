/* import { Router } from "express";
import { Response, Request } from "express";

const cardRouter = Router();

cardRouter.get("/:id", (req: Request, res: Response) => {
  // Getting cards from the api by their id
});

export default cardRouter;
 */
